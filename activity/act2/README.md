```bash
sudo apt install ncal
sudo apt install nano

usermod -aG sudo idhibhat

# 1
cal 2003

# 2
wc /etc/passwd -l

# 3
wc /etc/hosts -w

# 4
head /etc/passwd --lines=3

# 5
tail /etc/hosts --lines=4

# 6
grep idhibhat /etc/passwd
```